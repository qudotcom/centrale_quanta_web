fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
jinja2==3.1.2
python-multipart==0.0.6
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
# We removed the failing unitary library
# We can keep numpy/cirq if we want to build our own simple physics later
numpy
cirq-core
